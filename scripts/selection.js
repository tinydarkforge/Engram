// Selection — returns a tiered assertion schedule ranked by load-bearing score
// Exports: select({ task, caller_profile_id, plane_scope, budget, now }) => { tier0, tier1, tier2_refs, diagnostics }

module.exports = {};
