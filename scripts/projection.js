// Projection — renders a tier schedule into prompt fragments shaped per agent profile
// Exports: project(tierSchedule, profileId) => { fragments, omitted_tier2_refs, tokens_estimated }

module.exports = {};
